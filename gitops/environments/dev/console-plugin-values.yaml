plugin:
  image:
    tag: 1.0.8-feature

# Proxy the MCP server from the console plugin
mcpServer:
  enabled: true
  serviceName: aiobs-mcp-server-svc
  namespace: openshift-ai-observability
  port: 8085

