apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aiobs-platform-cluster-monitoring-config
  namespace: openshift-gitops
  annotations:
    # Matches `make enable-user-workload-monitoring` step (cluster-scoped config).
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_ORG/ai-observability-summarizer.git
    targetRevision: main
    path: gitops/manifests/cluster-monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

