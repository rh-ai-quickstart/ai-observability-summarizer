apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aiobs-app-react-ui
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_ORG/ai-observability-summarizer.git
    targetRevision: main
    path: deploy/helm/react-ui-app
    helm:
      releaseName: react-ui
      valueFiles:
        - ../../../gitops/environments/dev/react-ui-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-ai-observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

